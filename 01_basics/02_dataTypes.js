"use strict"; // Treat all JS code as newer version

// alert(3+3) We are using Node.js Not Browser
console.log(3+3);

let name = "Dipak"
let age = 19
let isLoggedIn = false
// number, bigint, boolean, null(stand-alone value), undefined, symbol(unique)

// object 

console.log(typeof null); // Object
console.log(typeof undefined); //unique
